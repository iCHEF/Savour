# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Savour' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Savour
  pod 'Lock', '~> 1.28'
  pod 'Auth0', '~> 1.1'
  pod 'ChameleonFramework'
  pod 'FontAwesome.swift'
  pod 'SimpleKeychain'
  pod 'RxSwift', '~> 3.0'
  pod 'RxCocoa', '~> 3.0'

  target 'SavourTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SavourUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
